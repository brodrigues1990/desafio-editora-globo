import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            light: '#015d82',
            main: '#004561',
            dark: '#03374c',
            contrastText: '#fff',
          },
    },
  });

  export default theme;